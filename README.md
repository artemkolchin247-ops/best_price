# 📊 Sales Forecasting & Price Optimization System

Система прогнозирования продаж и оптимизации цен на основе анализа эластичности спроса.

## 🚀 Возможности

- **Прогнозирование продаж** с использованием моделей Random Forest, LogLog и Poisson
- **Расчет эластичности спроса** с доверительными интервалами
- **Оптимизация цен** для максимизации прибыли
- **Анализ качества данных** и валидация моделей
- **Интерактивный интерфейс** на Streamlit

## 📁 Структура проекта

```
best_price/
├── ui/
│   └── app.py                 # Основной интерфейс Streamlit
├── src/
│   ├── models/
│   │   └── sales_forecast.py  # Модели прогнозирования
│   ├── optimizer/
│   │   └── bruteforce.py      # Оптимизация цен
│   └── ingestion/
│       └── excel_parser.py    # Загрузка данных
├── data/                      # Папка для данных
├── .gitignore
└── README.md
```

## 🛠️ Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ВАШ_USERNAME/best_price.git
cd best_price
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # macOS/Linux
# или
venv\Scripts\activate     # Windows
```

3. Установите зависимости:
```bash
pip install streamlit pandas numpy scikit-learn scipy openpyxl
```

## 🎯 Использование

1. Запустите приложение:
```bash
streamlit run ui/app.py
```

2. Откройте браузер по адресу `http://localhost:8501`

3. Загрузите данные в формате Excel с колонками:
   - `date` - дата
   - `price_before_spp` - цена до СПП
   - `orders` - количество заказов
   - `ad_internal`, `ad_bloggers`, `ad_vk` - рекламные бюджеты

## 📊 Модели

- **Random Forest** - универсальная модель для сложных зависимостей
- **LogLog** - логарифмическая модель для эластичности
- **Poisson** - модель для счетных данных (количество заказов)

## 🔍 Анализ

Система автоматически:
- Вычисляет локальную и глобальную эластичность
- Оценивает стабильность модели
- Определяет монотонность кривой спроса
- Применяет защитные режимы при низкой точности

## 📈 Оптимизация

Находит оптимальную цену для максимизации прибыли с учетом:
- Эластичности спроса
- Издержек (COGS, логистика, хранение)
- Рекламных бюджетов
- Комиссий и налогов

## 🛡️ Защитные режимы

- **Scenario** - только сценарный анализ при низкой точности
- **Conservative** - консервативная оптимизация
- **Normal** - полная оптимизация при высокой точности

## 📝 Лицензия

MIT License

## 🤝 Вклад

Pull requests приветствуются!
